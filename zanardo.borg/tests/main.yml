---
- hosts: localhost
  tasks:
    - include: ../../tests/prepare-ansible-test.yml
  
- hosts: ansible-test

  roles:
    
    - role: zanardo.borg
      borg_version: "1.0.7"

- hosts: localhost
  tasks:
    - include: ../../tests/destroy-ansible-test.yml
