---
- hosts: localhost
  tasks:
    - include: ../../tests/prepare-ansible-test.yml
  
- hosts: test-vms

  roles:
    
    - role: zanardo.borg
      borg_version: "1.0.7"

- hosts: localhost
  tasks:
    - include: ../../tests/destroy-ansible-test.yml
