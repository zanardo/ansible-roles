---
- hosts: localhost
  tasks:
    - include: ../../tests/prepare-ansible-test.yml
  
- hosts: ansible-test

  roles:
    
    - role: zanardo.gitolite
      gitolite_dir: /srv/git
      gitolite_version: "3.6.6"
      gitolite_admin_ssh_public_key: "test.pub"

- hosts: localhost
  tasks:
    - include: ../../tests/destroy-ansible-test.yml
