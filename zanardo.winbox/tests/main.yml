---
- hosts: localhost
  tasks:
    - include: ../../tests/prepare-ansible-test.yml
  
- hosts: test-vms

  roles:
    
    - role: zanardo.winbox
      winbox_version: "3.5"

- hosts: localhost
  tasks:
    - include: ../../tests/destroy-ansible-test.yml
