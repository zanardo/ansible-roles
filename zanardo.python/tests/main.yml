---
- hosts: localhost
  tasks:
    - include: ../../tests/prepare-ansible-test.yml
  
- hosts: test-vms

  roles:
    
    - role: zanardo.python
      python_versions:
        - 2.7.12
        - 3.4.5

- hosts: localhost
  tasks:
    - include: ../../tests/destroy-ansible-test.yml
