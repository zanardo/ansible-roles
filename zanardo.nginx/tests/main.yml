---
- hosts: localhost
  tasks:
    - include: ../../tests/prepare-ansible-test.yml
  
- hosts: ansible-test

  roles:
    
    - role: zanardo.nginx

- hosts: localhost
  tasks:
    - include: ../../tests/destroy-ansible-test.yml
