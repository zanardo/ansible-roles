---
- hosts: localhost
  tasks:
    - include: ../../tests/prepare-ansible-test.yml
  
- hosts: test-vms

  roles:
    
    - role: zanardo.go
      go_version: "1.7.1"

- hosts: localhost
  tasks:
    - include: ../../tests/destroy-ansible-test.yml
