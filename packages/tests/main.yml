---
- hosts:
    - debian-stretch
    - debian-buster
    - debian-bullseye
    - ubuntu-bionic

  vars:
    packages_install:
      - tmux
      - git
      - make

  roles:
    - { role: zanardo/packages }
