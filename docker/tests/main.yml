---
- hosts:
    - debian9

  vars:
    docker_daemon_config:
      "bip": "192.168.3.1/24"
      "ip-forward": no
      "ip-masq": no
      "iptables": no
      "hosts":
        - "unix://"
        - "tcp://0.0.0.0:2375"

  roles:
    - zanardo/docker
