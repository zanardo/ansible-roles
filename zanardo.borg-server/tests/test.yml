---
- hosts: test-vms

  roles:
    
    - role: zanardo.borg-server
      borg_version: "1.0.9"
      borg_server_ssh_keys:
        - name: fulano
          key: "ssh-rsa 123"
        - name: ciclano
          key: "ssh-rsa 321"
