---
- hosts: localhost
  tasks:
    - include: ../../tests/prepare-ansible-test.yml
  
- hosts: test-vms

  roles:
    
    - role: zanardo.rrd
      rrd_version: 0.3

- hosts: localhost
  tasks:
    - include: ../../tests/destroy-ansible-test.yml
