---
- hosts: localhost
  tasks:
    - include: ../../tests/prepare-ansible-test.yml
  
- hosts: ansible-test

  roles:
    
    - role: zanardo.rrd
      rrd_version: 0.3

- hosts: localhost
  tasks:
    - include: ../../tests/destroy-ansible-test.yml
