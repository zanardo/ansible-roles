---
- hosts: localhost
  tasks:
    - include: ../../tests/prepare-ansible-test.yml
  
- hosts: ansible-test

  roles:
    
    - role: zanardo.common
      apt_proxy: "http://aptcache:3142/"

- hosts: localhost
  tasks:
    - include: ../../tests/destroy-ansible-test.yml
